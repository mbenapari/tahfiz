name: Deploy to Production

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: tahfiz_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Build
        run: npm run build

      - name: Run Tests
        env:
          NODE_ENV: test
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: postgres
          DB_PASSWORD: postgres
          DB_NAME: tahfiz_test
          JWT_SECRET: test-secret
          ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
          CSRF_SECRET: test-csrf-secret
        run: |
          npx sequelize-cli db:migrate --env test
          npx sequelize-cli db:seed:all --env test
          npm test

      - name: Set up SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            # Ensure target directory exists and we have permissions
            sudo mkdir -p /opt/tahfiz
            sudo chown -R ${{ secrets.SERVER_USER }}:${{ secrets.SERVER_USER }} /opt/tahfiz
            cd /opt/tahfiz
            
            # Remove only the files we are about to replace, excluding hidden ones like .env
            # This is safer than rm -rf *
            find . -maxdepth 1 ! -name '.env' ! -name '.' -exec rm -rf {} +
            
      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          source: ".,!node_modules"
          target: "/opt/tahfiz"

      - name: Deploy on Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd /opt/tahfiz
            
            # Create .env file from secrets
            cat <<EOF > .env
            NODE_ENV=production
            DB_NAME=${{ secrets.DB_NAME }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            DB_HOST=${{ secrets.DB_HOST }}
            DB_PORT=${{ secrets.DB_PORT }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            ENCRYPTION_KEY=${{ secrets.ENCRYPTION_KEY }}
            REDIS_URL=${{ secrets.REDIS_URL }}
            CSRF_SECRET=${{ secrets.CSRF_SECRET }}
            DEFAULT_SYSTEM_OWNER_PHONE=${{ secrets.DEFAULT_SYSTEM_OWNER_PHONE }}
            DEFAULT_SYSTEM_OWNER_PHONE_PASSWORD=${{ secrets.DEFAULT_SYSTEM_OWNER_PHONE_PASSWORD }}
            EOF

            # Build and restart containers
            # Use sudo if the user isn't in the docker group
            sudo docker compose down --remove-orphans || true
            sudo docker compose up --build -d
            
            # Cleanup old images
            sudo docker image prune -f
